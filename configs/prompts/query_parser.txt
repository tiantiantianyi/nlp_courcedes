将用户的中文搜图请求解析为 JSON：raw_text、semantic_text、query_type、objects、actions、scene、mood、colors、style、required_terms、excluded_terms、ocr_terms。query_type 只能是 simple、compositional、negative、count、ocr 之一；其余分类字段均为字符串数组。semantic_text 删除否定条件但保留要检索的正向语义；识别“不要、排除、没有”等条件并放入 excluded_terms；引号中的招牌或文字放入 ocr_terms。只输出 JSON。
